import { DisruptionCockpit } from "@/components/DisruptionCockpit";

export default function HomePage() {
  return (
    <main className="min-h-screen px-3 py-4 md:px-6 md:py-6">
      <DisruptionCockpit />
    </main>
  );
}
